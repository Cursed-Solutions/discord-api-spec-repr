name: Publish documentation

on:
  workflow_dispatch:
  push:
    branches: [master]
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish-docs:
    uses: FasterSpeeding/Piped/.github/workflows/release-docs.yml@95603ca03f9eae72602cb5ff1f2c9dd97fb140fc
    secrets: inherit
