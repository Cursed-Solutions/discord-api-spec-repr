name: Publish documentation

on:
  workflow_dispatch:
  push:
    branches: [master]
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish-docs:
    uses: FasterSpeeding/Piped/.github/workflows/release-docs.yml@13c12d52c5d5e27d8a87398f14e469745117443b
    secrets: inherit
