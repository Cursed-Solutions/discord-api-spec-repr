name: Freeze PR dependency changes

on:
  pull_request:
    branches:
      - master
    paths: ["piped", "pyproject.toml"]

jobs:
  freeze-pr-dep-changes:
    uses: FasterSpeeding/Piped/.github/workflows/freeze-for-pr.yml@030a614a47058aded1fc89ab1c28ccd4259a5ce7
    secrets: inherit
