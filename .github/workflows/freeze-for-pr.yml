name: Freeze PR dependency changes

on:
  pull_request:
    branches:
      - master
    paths: ["piped", "pyproject.toml"]

jobs:
  freeze-pr-dep-changes:
    uses: FasterSpeeding/Piped/.github/workflows/freeze-for-pr.yml@45039ff4ec17015d4963e6cc9e1c6d7a21add80a
    secrets: inherit
