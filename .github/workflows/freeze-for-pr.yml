name: Freeze PR dependency changes

on:
  pull_request:
    branches:
      - master
    paths: ["piped", "pyproject.toml"]

jobs:
  freeze-pr-dep-changes:
    uses: FasterSpeeding/Piped/.github/workflows/freeze-for-pr.yml@9c2b284f6fb48a4b294728b63fa4032b15104e00
    secrets: inherit