name: Freeze PR dependency changes

on:
  pull_request:
    branches:
      - master
    paths: ["piped", "pyproject.toml"]

jobs:
  freeze-pr-dep-changes:
    uses: FasterSpeeding/Piped/.github/workflows/freeze-for-pr.yml@1ccc9c614a20fe0ec6c39c4de0b456b183dc386c
    secrets: inherit
